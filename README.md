
  # Computer-Based Test Website

 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ## Project docs

  Backend handoff and API guidance: `BACKEND_HANDOFF.md`
  
